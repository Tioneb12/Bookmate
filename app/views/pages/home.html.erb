<div class="banner" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.5) 50%), url('<%= image_path "banner.jpg" %>');">
  <div class="container">
    <%= render 'books/search'  %>
    <h4>Trouve les personnes qui ont les mêmes gouts littéraires que toi</h4>
    <h6>Selectionne 5 livres et tu verras qui se cache derrière.</h6>
  </div>
</div>
<div class="container">
  <div id="book-results"></div>
  <div id="display-none">
    <%= render collection: @books, partial: "books/book" %>
  </div>
  <% if (user_signed_in? && current_user.readings.count >= 5) || session[:books].count >= 5 %>
    <%= link_to "Aller voir les profils",  users_path, class: "btn btn-primary stat-display", id: "btn-path" %>
  <% else %>
    <%= link_to "Aller voir les profils",  users_path, class: "btn btn-primary stat", id: "btn-path" %>
  <%end %>
</div>
