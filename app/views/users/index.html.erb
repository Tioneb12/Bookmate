

  <% @users.find_each(:batch_size => 5) do |user| %>
  <ul>
    <li><%= link_to user.last_name , user_path(user.id) %></li>
    <li><%= user.first_name%></li>
    <li><%= user.address %></li>
  </ul>
  <% end %>
